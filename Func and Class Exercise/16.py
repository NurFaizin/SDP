#!/usr/bin/env python2.7
# -*- coding: utf-8 -*-

import re

print "Import multi password separated by comma:"
passwords = raw_input("> ").split(",")

for pasword in passwords:
    if (re.search('[^a-z^]').passwords):
        print passwords
